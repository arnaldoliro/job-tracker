/** Superfície pública da feature. \`api.ts\` e \`actions.ts\` são internos. */
export { UnlinkedInbox } from "@/features/emails/components/unlinked-inbox";
export { listUnlinkedEmails, getEmailStatus } from "@/features/emails/api";
